# Praktikum-Algoritma-Pemograman

# JOBSHEET 1 

<p>
Setelah saya mempelajari Jobsheet 1 ini saya mengetahui apa itu algoritma,pemrograman,bahasa pemrograman,programmer,IDE,cara menginstal IDE dan menampilkan Hello World! meunggunakan CMD. 
</p>

<p>
  Algoritma merupakan urutan langkah-langkah yang sistematis dan logis untuk menyelesaikan masalah.Dan algoritma harus berakhir setelah melakukan langkah-langkah,sehingga masalah dapat diselesaikan dan terstruktur.
</p>

<p>
  Pemrograman adalah proses pembuatan suatu program komputer dengan menggunakan bahasa pemrograman seperti kegiatan menulis,menguji,memperbaiki dan memelihara kode dengan menggunakan bahasa pemograman yang dapat dipahami oleh komputer.dan orang yang melakukan pemograman tersebut disebut programmer. 
</p>

<p>
  Bahasa pemrograman dalah kode yang berfungsi untuk memberi instruksi pada komputer.Bahasa pemograman memiliki tingkatan dari bahasa mesin,bahasa tingkat rendah,tingkat menengah dan tingkat tinggi,semakin mendekati tingkat bahasa mesin maka bahasa pemrograman semakin mudah untuk dimengerti komputer dan lebih cepat.Pda jobsheet ini menggunakan bahasa pemrograman C.
</p>

<p>
  Bahasa Pemograman ditulis ke dalam IDE atau Integrated Development Environment.IDE merupakan perangkat lunak untuk melakukan pemograman dalam membuat aplikasi.Karena kita menggunakan bahasa c saya memilih DevC++ dan  saya membuat program untuk mencetak "Hello World" melalui DevC++ yang akan ditampilkan memalui CMD.
</p>

# JOBSHEET 2
<p>
  Setelah Saya mempelajari Jobsheet 2 saya mengetahui tentang Pemrograman Dasar C mulai dari Struktur Dasar C,Proses Input dan Output,Fungsi input,Sintaks, dan Format Specifier.
</p>

<p>
  Struktur dasar Bahasa Pemrograman C kita wajib menggunakan dua bagian utama,yaitu Bagian inlcude dan blok fungsi main.Pada bagian include terdapat header files yang digunakan untuk menggunakan fungsi-fungsi yang terdapat pada file header tersebut.Blok fungsi main adalah fungsi utama dimana semua perintah-perintah kita dijalankan pertama kali saat program dijalankan.Perintah-perintah tadi disebut statement seperti printf("Hello World);.Pada bahasa c kita wajib menggunakan semicolon (;) untuk mengakhiri statement kita agar tidak terjadi error.Untuk mengidentifikasi variabel,fungsi,atau semua yang dapat didefenisikan  oleh kita sebagai programmmer kita menggunakan identifier yang merupakan sebuah nama.Penulisan identifier ini memiliki aturan-aturan yang harus diikuti.Pada bahasa c kita akan menggunakan comment yang digunakan untuk mendeskripsikan statement dari program,comment ini tidak akan dijalankan oleh komputer.Comment dapat ditulis menggunakan tanda // pada awal satu baris komentar dan kita dapat menggunakan tanda /* untuk awal baris dan tanda */ untuk akhir baris jika kita ingin menulis komentar lebih dari satu baris.
</p>

<p>
  Proses Input dan Output.Input adalah data yang kita masukkan melalui perangkat keras inputan,seperti keyboard,microphone dan mouse.Proses merupakan langkah-langkah yang dijalankan program untuk menghasilkan output.Output merupakan hasil dari program yang dijalankan dan tampil di layar komputer.Untuk menampilkan output di dalam bahasa c kita dapat menggunkaan fungsi printf dan fungsi puts.perbedaan dari kedua fungsi ini fungsi printf jika ingin menampilkan output kitaharus menggunakan format yang sesuai dengan variabel atau apa yang ingin kita tampilkan.Fungsi Input pada bahasa c.Pada bahasa c terdapat fungsi input seperti fungsi scanf,gets,fgets.
</p>

# JOBSHEET 3
<p>
  Setelah mempelajari Jobsheet 3 saya mengetahui tentang variabel,konstanta dan tipe data pada bahasa c.
</p>

<p>
  Variabel adalah suatu tempat untuk menyimpan suatu nilai yang memiliki tipe data tertentu.Saat menulis nama variabel kita harus mengikuti aturan seperti tidak boleh menggunakan nama yang sudah ada di bahasa c seperti if,void dan lain lain.
</p>

<p>
  Tipe data adalah jenis data atau nilai yang akan disimpan di dalam variabel,seperti integer,char,float,double dan lain lain.Tipe data terbagi menjadi empat kelompok,yaiut tipe data dasar,tipe data void,tipe data turunan,dan tipe data enum atau bentukan.Kita juga dapat melakukan konversi tipe data.Konversi tipe data adalah mengubah tipe data menjadi tipe data yang lain,contohnya kita dapat mengubah tipe data integer menjadi float dengan menuliskan tipe datanya di depan variabel yang tipe datanya ingin kita ubah.
</p>

<p>
  Konstanta adalah nilai tetap,maksudnya variabel yang dideklarasikan menjadi variabel konstanta tidak dapat diubah nilainya.Dalam mendeklarasikan variabel konstanta terdapat dua cara di bahasa c,yaitu dengan menggunakan #define di awal variabel contohnya #define BERAT 10.Cara pertama ini dilakukan di luar fungsi main.Cara kedua menggunakan const di awal dan berada sebelum tipe data variabel contohnya const int berat = 10;.Cara ini bisa di tulis di luar maupun di dalam fungsi main.
</p>

# JOBSHEET 4
<p>
  Setelah mempelajari Jobsheet 4 ini saya mengetahui apa itu operator dan jenis jenis operator yang terdapat di bahasa c.Operator adalah sebuah simbol yang digunakan untuk melakukan operasi tertentu.Operator terbagi menjadi 3 berdasarkan jumlah operandnya,Operator unary memiliki 1 operand seperti increment dan decrement,operator binary memiliki 2 operand seperti aritmatika,operasi perbandingan dan operasi hubungan dan yang terakhir operator ternary memiliki 3 operand seperti operasi ternary.
</p>

<p>
  Operator aritmatika adalah operator yang melakukan operasi matematika seperti penjumlahan memiliki simbol (+),pengurangan memiliki simbol (-),perkalian memiliki simbol (*),pembagian memiliki simbol (/) dan modulus memiliki simbol (%).
</p>
<p>
  Operator penugasan merupakan operator yang digunakan untuk memberikan nilai ke suatu variabel.Maksudnya kita dapat menginisialisasikan atau mengubah nilai dari variabel contoh operator penugasan,seperti operator pengisian nilai memiliki simbol (=),pengisian dan penambahan memiliki simbol (+=),pengisian dan pengurangan memiliki simbol (-=) dan lain lain.
</p>

<p>
  Operator pembanding,operator ini membandingkan dua buah nilai,seperti operator lebih besar simbolnya (>),lebih kecil simbolnya (<) dan lain lain.Operator ini menghasilkan nilai berupa true atau 1 dan false atau 0.
</p>

<p>
  Operator Logika,operator ini digunakan untuk melakukan operasi logika seperti logika AND simbolnya (&&),logika OR simbolnya (||) dan negasi atau kebalikan simbolnya (!).Operator ini menghasilkan nilai true dan false.
</p>

<p>
  Operator bitwise,operator ini digunkan untuk melakukan operasi berdasarkan biner dari nilai,seperti AND (&),OR (|),XOR (^), NOT (~),Left shift (<<),dan Right shift (>>).Dan masih banyak lagi operator yang terdapat di bahasa c,seperti alamat memory,pointer,ternary,increment dan decrement.
</p>

# JOBSHEET 5

<p>
  Pada Jobsheet ini saya mempelajari mengenai percabangan atau decision making yang berguna untuk pengambil keputusan sesuai kondisi yang kita atur,seperti if,if else,switch case,operator ternary,dan percabangan bersarang.
</p>

<p>
  If merupakan percabangan yang hanya memiliki satu blok yang akan dijalankan jika kondisinya benar.if else merupakan percabgnan yang memilki dua blok.Blok pertama akan dijalankan jika kondisi benar dan Blok ke dua akan berjalan ketika kondisi salah.if esle if merupakan percabangan yang memiliki lebih dari dua blok pilihan sedikit mirip dengan if else.Namun if else if memiliki kondisi yang banyak sesuai dengan kondisi yang kita inginkan.Kita juga dapat membuat percabangan bersarang seperti if else di dalam if.
</p>

<p>
  Switch case merupakan percabangan yang mirip dengan percabangan if else if.Switch case juga bisa juga memiliki kondisi sebanyak yang kita inginkan.setiap case harus diakhiri dengan break.Jika pada default kita tidak menggunakan break,karena default terletak di akhir.Break berguna untuk membuat program berhenti mengecek kondisi berikutnya ketika kondisi pada case sebelumnya sudah benar.
</p>

<p>
  Percabangan ternary merupakan percabgnan yang menggunkan operator ternary.Percabangan ini seperti if else,karena hanya memiliki satu kondisi.setelah kita membuat kondisnya kita harus menggunkaan tanda tanya (?) setelah tanda tanya kita akan mengisi perintah jika kondisinya benar dan perintah berikutnya jika kondisi salah.Kedua perintah ini dipisah menggunakan tanda ":". 
</p>

# JOBSHEET 6

<p>
  Pada jobsheet 6 saya mempelajari tentang loop atau perulangan pada bahasa c.Perulangan adalah aliran kontrol yang mengatur kode untuk dijalankan berulang kali hingga kondisi perulangan tidak terpenuhi dan perulangan berhenti.dan tujuan dilakukan perulangan ini untuk membuat program menjadi lebih ringkas.Perulangan terbagi menjadi dua jenis,yaitu perulangan terhitung atau counted loop,seperti perulangan for dan perulangan tidak terhitung atau uncounted loop,seperti perulangan while dan perulangan do while.
</p>
<p>
  Perulangan for merupakan perulangan yang termasuk perluangan terhitung atau counted loop,karena berapa banyak perulangan ini dilakukan sudah ditentukan melalui program.Perulangan for memiliki kondisi yang harus dipenuhi agar perulangan ini berjalan.dan saat perulangan ini dijalankan ada variabel yang bertugas sebagai counter yang akan menjadi acuan kondisi pada perulangan for ini.Perulangan for ini bisa dilakukan secara bersarang atau nested loop.Maksudnya di dalam blok perulangan for bisa terdapat perulangan for lagi dan setersunya sesuai kebutuhan kita.
</p>

<p>
  Perulangan while merupakan perulangan yang termasuk perulangan tidak terhitung atau uncounted loop.Namun perulangan ini bisa menjadi counted loop dengan memberi counter di dalamnya.
</p>

<p>
  perulangan do while sama seperti perulangan while,termasuk perulangan tidak terhitung atau uncounted loop.Namun perulangan do while akan melakukan perulangan 1 kali lalu mengecek kondisi yang terdapat di while.
</p>

# JOBSHEET 7
<p>
  Pada Jobsheet 7 saya mempelajari tentang pengertian array,fungsi dari array,array multidimensi dan string pada bahasa c.Array merupakan struktur data yang berguna untuk menyimpan banyak data dalam satu variael.Array memiliki indeks yang dimulai dari 0.Pada bahasa c kita menulis array menggunkan tanda [] untuk menentukan panjang dari array.
</p>

<p>
  Array dapat diakses dengan mengakses indeks dari array tersebut,contohnya array[1],artinya kita mengakses array indeks ke-1.dengan mengakses indeks array tersebut kita dapat menampilkan dan merubah isi dari indeks array tersebut.Agar kita dapat mengakses indeks array dengan mudah kita dapat menggunakan perulangan. Dan kita dapat mengetahui panjang dari array dengan menggunkan sizeof()
</p>

<p>
  Array multidimensi merupakan array yang dapat menyimpan array lagi seperti membuat matriks.Dan string merupakan contoh dari penggunaan array 1 dimensi dengan tipe data char.kita dapat memanipulasi string dengan fungsi-fungsi yang tersedia di bahasa c seperti strcpy,strcat,strlen dan lain lain.Fungsi-fungsi ini terdapat di file header string.h. 
</p>

# JOBSHEET 8
<p>
  Pada Jobsheet 8 ini saya mempelajari penertian fungsi,cara menggunakan fungsi,fungsi rekursif,pass by value dan pass by reference.Fungsi merupakan blok-blok program dengan nama tertentu yang dapat kita panggil berkali-kali dalam program.
</p>

<p>
  Fungsi biasanya mengembalikan nilai,ketika fungsi digunakkan untuk mengembalikan nilai maka di dalam fungsi tersebut harus menggunakan kata kunci return di akhir.Namun ada fungsi yang tidak mengembalikan nilai,fungsi ini menggunkaan tipe void.Fungsi yang tidak mengembalkan nilai biasanya hanya digunakan untuk menampilkan suatu nilai.
</p>

<p>
  Fungsi dengan Parameter.Fungsi ini menerima nilai dari variabel saat dipanggil.Parameter akan memberi nilai ke fungsi sesuai denga tipe data yang diminta ketika fungsi tersebut didefenisikan.Cara memberikan nilai pada parameter terdapat dua cara,yaitu pass by value dan pass by reference.pass by value kita bisa langsung memasukkan nilai ke fungsi,sedangkan passs by reference kita memberikan alamat memori ketika fungsi dipanggil tujuannya untuk langsung merubah isi dari variabel yang dijadikan sebagai parameter.
</p>

<p>
  Fungsi rekursif merupakan fungsi yang memanggil dirinnya sendiri.Fungsi ini digunakan untuk melakukan perulangan ketika dipanggil.
</p>

<p>
  Variabel global merupakan variabel yang bisa diakses oleh semua fungsi.variabel ini dideklarasikan diluar fungsi main,Sementara variabel lokal dideklarasikan di dalam fungsi tertentu dan hanya daapat diakses di dalam fungis tersebut.
</p>

# JOBSHEET 9
<p>
  Pada Jobsheet 9 membahas tentang pointer di bahasa c.Pointer merupakan variabel yang berfungsi untuk menyimpan alamat memori dari variabel lain.Saat membuat variabel pointer kita menggunakan simbol * di tengah-tengah antara nama variabel dan tipe data dari variabel tersebut.
</p>
<p>
  Kegunaan dari pointer ini adalah untuk mengakses data atau nilai yang terdapat di alamat memori yang sudah kita isi sebagai nilai variabel pointer.Jika kita ingin melihat alamat memori dari suatu variabel kita dapat menggunaan tanda & diawal variabel yang akan kita jadikan sebagai nilai dari variabel pointer.
</p>
<p>
  Alasan mengapa kita menggunakan pointer agar kecepatan dan peforma dari program kita lebih bagus,karena dengan menggunakan pointer kita dapat menghemat penggunaan memori.
</p>

# JOBSHEET 10
<p>
  Pada Jobsheet 10 membahas tentang enum dan struktur.Enum merupakan tipe data bentukan yang berisi kumpulan konstanta.Jika kita tidak mengisi enum dengan angka konstanta, maka secara otomatis enum akan mengisi dengan nilai konstanta sesuai dengan urutan dari elemen yang kita isi.Enum digunakan untuk mendapatkan efiensi dan fleksibilitas memori.
</p>

<p>
  Struktur atau struct adalah tipe data bentukan yang dapat diisi variabel dengan tipe data yang beragam.Struct dapat dibuat dengan memberikan nama atau kata kunci setelah kata struct saat mendeklarasikan tipe data bentukan struct. 
</p>

<p>
  Selain memberikan nama atau kata kunci setelah kata struct saat mendeklarasikan tipe data bentukan,kita dapat menggunakan kata kunci typedef sebelum kata struct  saat mendeklarasikan agar ketika kita membuat variabel dengan tipe data bentukan struct tersebut kita hanya memanggil nama dari struct tersebut.
</p>

# JOBSHEET 11

<p>
  Pada Jobsheet 11 membahasa tentang file handling di dalam bahasa c.File handling ini berguna ketika kita menjalankan suatu program dan menginputkan nilai dari suatu variabel maka nilai dari variabel tersebut dapat disimpan di dalam file.
</p>

<p>
  File tebagi menjadi dua jenis ,yaitu file teks dan file binary.File teks memiliki ekstensi .txt, .html dan lain lain.File binary merupakan file yang tersimpan dalam bentuk biner dan sulit untuk dibaca oleh manusia.Namun menyimpan data lebih banyak dan aman dari pada file teks.File ini memiki ekstensi , seperti .exe dan .bin
</p>

<p>
  Kita bisa menggunakan fungsi-fungsi yang terdapat di bahasa c untuk membuka atau membaca file.Kita menggunakan fungsi fopen() dengan mode yang kita inginkan,seperti mode r untuk membaca file.Kita dapat membaca isi dari suatu file dengan menggunakan fungsi fgets() dan untuk menampilkan isi dari file yang memiliki isi lebih dari 1 baris kita dapat melakukan perulangan.Kita juga dapat mengisi isi file menggunakan fungsi fputs() dan kita dapat menggunakan perulangan untuk mengisi file tersebut lebih dari 1 baris.
</p>
